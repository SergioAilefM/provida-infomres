import React from 'react'
import { render } from '@testing-library/react'

import {ButtonSeguridad} from './ButtonSeguridad'

test('renders all options passed to it', () => {
    render(<ButtonSeguridad />)
})