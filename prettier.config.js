module.exports = {
    singleQuote: true, // Force using single quotes
    trailingComma: 'all', // Force the use of a comma at the end of objects
    arrowParens: 'avoid', // Force not using parentheses with single parameter functions.
    semi: false, // Force not using semicolons
    tabWidth: 4, // Force using 4 spaces for indentation
    bracketSameLine: true, // Force using bracket on the same line as the object
    singleAttributePerLine: true, // Force using single attribute per line
}
