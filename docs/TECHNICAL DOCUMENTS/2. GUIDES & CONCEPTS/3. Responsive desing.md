El soporte para responsive desing esta soportado sobre:

1. **Uso de Media Queries**: Las media queries son fundamentales para el diseño responsive. Permiten aplicar estilos específicos en función del tamaño de la pantalla del dispositivo. En SASS, puedes anidar media queries dentro de tus selectores para mantener el código organizado.

Aquí tienes un la implementacion de los breakpoints y mixin's estos estan definidos en el design system y pueden consumirse desde la web app:

```scss
$breakpoints: (
    sm: 26.375rem,
    // 422px
    md: 48rem,
    // 768px
    lg: 85.375rem,
    // 1366
    xl: 85.376rem,
    // 1366 and more
);

@mixin desktop {
    @media (min-width: map-get($breakpoints, 'xl')) {
        @content;
    }
}

@mixin laptop {
    @media (min-width: map-get($breakpoints, 'md')) and (max-width: (mapget($breakpoints, 'lg'))) {
        @content;
    }
}

@mixin tablet {
    @media (min-width: map-get($breakpoints, 'sm')) and (max-width: (mapget($breakpoints, 'md'))) {
        @content;
    }
}

@mixin phone {
    @media ((max-width: map-get($breakpoints, 'sm'))) {
        @content;
    }
}
```

Uso en Home.scss:

```scss
@import '@ds.e/scss/src/foundation/all';

.mo-homepage-container {
    // Desktop

    margin-top: 11.813rem;

    max-width: 1080px;

    margin-left: auto;

    margin-right: auto;

    @include tablet {
        margin-top: 10.313rem;

        margin-left: 20px;

        margin-right: 20px;
    }

    @include phone {
        margin-top: 0px;

        margin-left: 1rem;

        margin-right: 1rem;
    }

    .mo-homepage-floating-circle {
        position: absolute;

        left: 0px;

        top: 70px;

        z-index: -1;

        width: 50%;

        @media (max-width: 1000px) {
            top: 39px;

            width: 75%;
        }

        @media (max-width: 480px) {
            top: 39px;

            width: 100%;
        }

        img {
            width: 100%;

            height: auto;
        }
    }

    .mo-greeting-container {
        display: flex;

        width: 100%;

        height: auto;

        flex-direction: column;

        @include phone {
            margin-top: 5.438rem;
        }

        .mo-greeting-title-container {
            display: flex;

            h4 {
                margin: 0px;

                font-size: 32px;

                font-weight: 500;

                line-height: 150%;

                font-family: $metlife-circular-medium;

                @include tablet {
                    font-family: $metlife-circular-medium;

                    font-size: 30px;
                }

                @include phone {
                    font-family: $metlife-circular-medium;

                    font-size: 22px;

                    color: $metlife-gray-darkest;
                }
            }
        }

        .mo-subtitle-container {
            display: flex;

            p {
                margin: 0px;

                font-family: $metlife-circular;

                font-weight: 400;

                font-size: 32px;

                line-height: 40px;

                color: $metlife-gray-darkest;

                @include tablet {
                    font-size: 30px;
                }

                @include phone {
                    font-size: 22px;

                    line-height: 33px;
                }
            }
        }
    }
}
```

2. **Uso de viewportSize**: Se almacena dentro del contex de la app esto permite al usuario agregar logica dentro de los componentes o pages(controlers), para consumo de assets o renderizado de componentes relacionados a un dispositivo.

```ts
import React, { ReactNode, useEffect, useState } from 'react'

import DocumentTitle from 'react-document-title'

import { PAGE_TITLE } from '@constants/pageTitle'

import { PATHS } from '@constants/paths'

import FeaturedServiceCard from '@molecules/FeaturedServiceCard'

import { useNavigate } from 'react-router-dom'

import { DeviceName } from '@ds.e/foundation'

import DisclaimerModal from '@molecules/DisclaimerModal/DisclaimerModal'

import Loader from '@atoms/Loader'

import { LOADER_LABELS } from '@constants/loaderLabels'

import { useAppContext } from '@hooks/useAppContext'

import { useAdobeAnalytics } from '@hooks/useAdobeAnalytics'

import { useError } from '@hooks/useError'

import { useGetSpecialtiesSample } from '@services/specialtiesSample/getSpecialtiesSample/useGetSpecialtiesSample'

import HomeFloatingCircle from '../../../../assets/svg/HomeFloatingCircleDesktop.svg'

import '../../templates/home/Home.scss'

const FloatingCircle = ({ device }: { device: DeviceName }) => (
    <div className="mo-homepage-floating-circle">
        {!!device && <img alt="floating-circle" src={HomeFloatingCircle} />}
    </div>
)

const HomeContainer = ({
    children,

    device,
}: {
    children: ReactNode

    device: DeviceName
}) => (
    <div className="mo-homepage-container">
        <FloatingCircle device={device} />

        {children}
    </div>
)

const HomePage = () => {
    const { registerViewData } = useAdobeAnalytics()

    const {
        appState: { viewportSize, error, showLoading, ...appState },

        setShowLoading,
    } = useAppContext()

    const {
        error: errorSpecialties,

        isFetched: isFetchedSpecialties,

        data: dataSpecialties,
    } = useGetSpecialtiesSample()

    const [isModalOpen, setIsModalOpen] = useState(false)

    const navigate = useNavigate()

    const { errorHandler } = useError()

    const handleNavigateVideoCall = () => {
        navigate(PATHS.VIDEOCALL, {
            state: {
                specialty: 'Medicina General',

                previousPage: PATHS.HOME,
            },
        })
    }

    useEffect(() => {
        let statusCode = ''

        if (errorSpecialties) {
            statusCode = errorSpecialties?.message

                .split('status code')[1]
                .replaceAll(' ', '')
        }

        if (statusCode !== '') {
            errorHandler(statusCode)

            navigate(PATHS.ERROR)
        }
    }, [errorSpecialties])

    useEffect(() => {
        registerViewData('HOME', 'page')

        setShowLoading(true)
    }, [])

    useEffect(() => {
        if (isFetchedSpecialties) {
            setShowLoading(false)
        }
    }, [isFetchedSpecialties])

    return (
        <DocumentTitle title={PAGE_TITLE.HOME}>
            <HomeContainer device={viewportSize}>
                {showLoading ? (
                    <Loader message={LOADER_LABELS.MESSAGE_TEXT} />
                ) : (
                    <>
                        <DisclaimerModal
                            open={isModalOpen}
                            handleCloseModal={() => setIsModalOpen(false)}
                            handleNavigate={handleNavigateVideoCall}
                        />

                        {!!dataSpecialties && (
                            <FeaturedServiceCard
                                featuredServices={
                                    dataSpecialties.specialtiesSample
                                }
                                redirectCallback={() => setIsModalOpen(true)}
                            />
                        )}
                    </>
                )}
            </HomeContainer>
        </DocumentTitle>
    )
}

export default HomePage
```
